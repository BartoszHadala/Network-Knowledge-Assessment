# rsyslog rule to capture LOG_LOCAL0 messages for the daemon
# Install this file as /etc/rsyslog.d/99-mydaemon.conf (requires root)
# Format: selector  /path/to/logfile

local0.*    /var/log/mydaemon.log
